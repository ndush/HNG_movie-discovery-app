[build]
  command = "npm run build"
  publish = "dist"

# Order matters: the API rule must come before the SPA fallback.
[[redirects]]
  from = "/api/tmdb"
  to = "/.netlify/functions/tmdb"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
